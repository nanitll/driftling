//! Окно настроек Driftling (ТЗ §3.3, этап M0.5).
//!
//! РЕАЛИЗУЕТСЯ: eframe/egui-приложение (зависимость добавить сюда, не в
//! workspace). Требования:
//! - статус демона: опрос `driftling_ipc::call(Status)` ~раз в секунду
//!   (не каждый кадр!), плашка «демон не запущен», если сокет недоступен;
//! - кнопки: Призвать / Убрать / Остановить демон;
//! - настройки из `driftling_core::Config`: размер питомца (32–256),
//!   скорость ходьбы (5–400), непоседливость (w_idle_to_walk 0–100),
//!   сонливость (w_idle_to_sleep), длительность сна (min/max);
//! - «Сохранить и применить»: Config::save() + ipc call(Reload), показать
//!   результат; «Сбросить» — вернуть дефолты;
//! - тумблер автозапуска: создать/удалить ~/.config/autostart/driftling.desktop
//!   (Exec = текущий путь к driftling-бинарю рядом с настройками);
//! - подписи на русском (egui-шрифт Ubuntu-Light поддерживает кириллицу).

fn main() {
    eprintln!("driftling-settings: окно настроек ещё в разработке (M0.5)");
    std::process::exit(1);
}
